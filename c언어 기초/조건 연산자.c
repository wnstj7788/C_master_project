/*
#include <stdio.h>

int main(void){
  int x = -50, y = -30;

  int absoluteX = (x > 0) ? x : -x ;
  //조건 연산자 
  //x > 0 이 참값이라면 x 가 반환
  // x > 0 이 참이 아니라면  -x  반환
  int max = (x > y) ? x : y;
  int min = (x < y) ? x : y;
  printf("X의 절댓값은 %d 입니다\n",absoluteX);
  printf("x와 y의 최대값은 %d 입니다\n", max);
  printf("x와 y의 최솟값은 %d 입니다\n", min);

  return 0;
}
*/