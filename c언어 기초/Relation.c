
/*
#include <stdio.h>

int main(void){
  int x = 50, y = 30;
  //x가 y랑 같은 값을 가지고 있는가?
  //  x = y -> 수학에서는  x와 y가 같은 상황 그 자체
  // 코딩에서는 x == y 같은 값을 가지면 위와 같이 표현
  printf("x가 y와ㅣ 같은가? %d\n", x == y); // 0 flase를 의미

  printf("x가 y와 다른가? %d\n", x != y);
  printf("x가 y 큰가 ? %d\n", x > y);
  
  printf("x가 y보다 작은가? %d\n", x < y);

  printf("x에 y를 넣으면? %d\n", x = y);

  if ( x== y){
    // 실행
  }
  else{
    
  }
  
  

  return 0;
}
*/